[pytest]
# 环境里用户装的 dash 包注册了 pytest 插件但其依赖 typing_extensions 过旧，
# 导入即失败会让 pytest 起不来（colcon test 报 pytest.missing_result），显式禁掉。
addopts = -p no:dash
testpaths = test
markers =
    integration: 慢速集成测试（会拉起真实节点）
