[pytest]
# 环境里用户装的 dash 包注册了 pytest 插件 (dash.testing.plugin)，但它的依赖
# typing_extensions 版本太旧（缺 NotRequired），导入即失败，会让 pytest 整个
# 起不来（症状：colcon test 报 "pytest.missing_result"）。本工程用不到它，显式禁掉。
addopts = -p no:dash
testpaths = test
